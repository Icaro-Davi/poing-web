import { BOT } from "../../defaultBoTInfo";

const dashboard = {
    buttonsArea: {
        home: 'Home',
        logout: 'Logout'
    },
    navigationDashboardButtons: {
        [BOT.name]: BOT.name,
        commands: 'Commands',
        modules: 'Modules'
    }
}

export default dashboard;