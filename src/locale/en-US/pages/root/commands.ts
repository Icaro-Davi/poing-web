import { BOT } from "../../../defaultBoTInfo";

const commands = {
    head: {
        title: `${BOT.name} | All functional commands to help you on your server`,
        description: `Here are all the commands that will be available in ${BOT.name}, with them you will be able to manage your guild/server much better, enjoy!`
    }
}

export default commands;