type LocaleStaticValidation = {
    required: string;
    maxLength: string; // string var {%value%}
    onlyNumbers: string;
    minNumber: string; // string var {%value%}
    maxNumber: string; // string var {%value%}
}

export default LocaleStaticValidation;