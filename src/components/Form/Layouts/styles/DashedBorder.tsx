import styled from "styled-components";

const DashedBorder = styled.div`
    border: 3px dashed #FFF;
    width: 100%;
    `;

export const DashedBorderDivider = styled.div`
    border-bottom: 3px dashed #FFF;
`;

export default DashedBorder;